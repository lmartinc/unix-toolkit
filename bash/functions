mkcd()
{
    mkdir -p "$1"
    cd "$1"
}

extract()
{
    if [ -f "$1" ]; then
        case "$1" in
            *.tar.gz) tar xzf "$1" ;;
            *.zip) unzip "$1" ;;
            *.tar) tar xf "$1" ;;
            *) echo "Unsupported format" ;;
        esac
    fi
}

