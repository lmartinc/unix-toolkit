Install Git

Clone repository

Run install.sh

Verify installation

Done

## Corporate Proxy Networks

In restricted enterprise environments GitHub access may require HTTPS
instead of SSH.

Configure Git proxy settings:

git config --global https.proxy http:username:password@//proxy.server:port
git remote -v 
